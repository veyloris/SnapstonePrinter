# KSP build exception: local patch validation

Created: 2026-09-15. State: started; local checks complete, independent review and PR
handling remain with the main thread.

## Premises

- **Measured:** `git rev-parse HEAD` returned
  `92abee67a84836990538e99f0715a15bd82613ae` before the patch on 2026-09-15.
- **Measured:** `rg -n 'googleDevtoolsKsp' gradle/libs.versions.toml` returned version
  `2.3.5` before the patch and `2.3.6` after it on 2026-09-15.
- **Inherited:** the main thread ran the before command below in this checkout with
  the stated JDK/SDK environment and reported exit 0; the executor independently read
  the resulting log and observed the exception and successful build lines below.
- **Inherited:** the main thread identified 2.3.6 as the targeted fix from
  [the KSP maintainer's issue comment](https://github.com/google/ksp/issues/2763#issuecomment-3890131997).
  Use the local before/after checks below to assess this checkout's result.

## Decision and scope

Apply only the KSP patch version to address the observed background exception without
changing unrelated dependencies, application behavior, or suppressing build diagnostics.
Keep the CI baseline in its separate PR.

## Before/after appendix

Command for both observations, run from
`/home/veyloris/git/SnapstonePrinter-worktrees/ksp-fix` on 2026-09-15:

```bash
JAVA_HOME=/home/veyloris/.local/share/snapstone-review/jdk-25 ANDROID_HOME=/home/veyloris/Android/Sdk bash gradlew --no-daemon clean :app:testDebugUnitTest :app:lintDebug :app:assembleDebug --console=plain
```

Before output in `/tmp/snapstone-ksp-before.log`, inspected with
`rg -n 'ApplicationManager.getApplication|BUILD SUCCESSFUL'`:

```text
126:Exception in thread "AWT-EventQueue-0" java.lang.NullPointerException: Cannot invoke "ksp.com.intellij.openapi.application.Application.getService(java.lang.Class)" because the return value of "ksp.com.intellij.openapi.application.ApplicationManager.getApplication()" is null
160:BUILD SUCCESSFUL in 44s
```

After output in `/tmp/snapstone-ksp-after.log`, with executor-observed exit 0:

```text
148:BUILD SUCCESSFUL in 1m 6s
56 actionable tasks: 56 executed
Configuration cache entry stored.
```

The same `rg` query returned no `ApplicationManager.getApplication` match in the after
log. Do not interpret these single build durations as a performance comparison.

After completion, a Node check read every test XML suite's `tests`, `failures`, and
`errors` attributes under `app/build/test-results/testDebugUnitTest/`, counted
`Error`/`Fatal` severities in `app/build/reports/lint-results-debug.xml`, required a
nonempty `app/build/outputs/apk/debug/app-debug.apk`, and rejected any
`ApplicationManager.getApplication()` occurrence in the after log; it exited 0 with:

```json
{"xmlFiles":8,"tests":76,"failures":0,"errors":0,"lintErrors":0,"apkBytes":66870422,"kspException":false}
```

The executor's `git diff --check` returned exit 0 on 2026-09-15. A report check attempted
before build completion found the lint XML absent; the result above is from the repeat
after the build exited successfully.

## Limits

Unverified: inherited before-command provenance and the upstream fix explanation were
supplied by the main thread; the executor inspected the log and measured the after run.
Treat disappearance of this exception as this checkout's clean-build observation, not
a guarantee about every KSP environment. Emulator and physical printer tests are outside
this patch's validation scope.
